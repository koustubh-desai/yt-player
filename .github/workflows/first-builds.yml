name: Publish Packages

on:
  push:
    branches:
        - feat/learna

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20.8'

      - name: Install dependencies
        run: npm ci

      - name: Authenticate with npm
        env:
            NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
        run: echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" > ~/.npmrc

      - name: Build and Publish header_by_kd package
        #if: ${{ github.event_name == 'push' && contains(github.event.commits[0].added, 'packages/header_by_kd') }}
        run: npx lerna exec --scope header_by_kd -- npm run build && npx lerna exec --scope header_by_kd -- npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}

      - name: Build and Publish footer_by_kd package
        #if: ${{ github.event_name == 'push' && contains(github.event.commits[0].added, 'packages/footer_by_kd') }}
        run: npx lerna exec --scope footer_by_kd -- npm run build && npx lerna exec --scope footer_by_kd -- npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}

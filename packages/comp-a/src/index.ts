export class CompA {
    // Define any methods or properties your component might have (optional)
  
    public someMethod(arg1: string): string {
      // Implement some dummy logic
      return `Dummy output for argument: ${arg1}`;
    }
  }
  